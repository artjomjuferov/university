#include <stdio.h>
#include <string>
#include <stdlib.h>
#include <math.h>
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

#define MAX 100000
struct list
{};




int main()
{
  FILE *ifp, *ofp;
  ifp = fopen("input.txt", "r");
  ofp = fopen("output.txt", "w+");

  int n, m;
  fscanf(ifp, "%d %d", &n, &m);

  printf("%d",D[n]);

  return 0;
}
